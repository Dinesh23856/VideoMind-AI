# Core web & orchestration
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
celery[redis]>=5.4.0
redis>=5.2.0
python-multipart>=0.0.12
pydantic>=2.9.0
pydantic-settings>=2.6.0
httpx>=0.27.0
aiofiles>=24.1.0

# Media ingestion
yt-dlp>=2024.12.0
ffmpeg-python>=0.2.0

# Speech-to-text
faster-whisper>=1.1.0
# silero-vad is pulled in via torch / torchaudio if needed; faster-whisper has built-in VAD

# LLM clients
openai>=1.55.0
anthropic>=0.39.0
google-generativeai>=0.8.0

# Artifact generation
genanki>=0.13.1
notion-client>=2.2.1
typst>=0.12.0
markdown>=3.7
python-dotenv>=1.0.1

# Utilities
tenacity>=9.0.0
structlog>=24.4.0
orjson>=3.10.0
typer>=0.14.0
rich>=13.9.0

# Optional: local GPU acceleration helpers
# torch  # install separately for CUDA if self-hosting Whisper
